# sub_08023BC0 (0x08023BC0, 936 bytes) — DEFERRED (pure-C constraint run, 2026-06-10)

Two-session attempt under a no-pin/no-asm constraint; worktree reverted clean.

## Drift
- Best pure-C: **byte_diff 103** (size exact at 936, pools placed correctly; prior
  session's plateau was 282). Residual = 4 clusters, ALL the const-copy /
  orientation class (family of sibling sub_08021510, which shipped with a pin):
  1. case-1 status test: baserom materializes 0x8000 into a scratch then copies
     (`movs r3,#128; lsls r3,#8; adds r0,r3,#0; ands r0,r1`); gcc 2.7
     `find_dummy_reload` ("See if OUT will do") + push_reload equiv-const
     substitution always remat a single-use const into the output — the 4-insn
     shape is UNCONSTRUCTIBLE in pure C.
  2. case-2 mask AND: const-side copy (`adds r0,r2,#0; ands r1,r0`) only arises
     for `(subreg:SI (reg:HI const))` operands; `f-1` is dynamic — unconstructible.
  3. 0xFDFF store block: r0/r1/r2 scratch rotation (~20 B).
  4. Assorted single-register colorings.
- Flag sweep (old_agbcc = gcc 2.7: no gcse/scheduler): ALL no-ops.
  ~150k permuter iterations: one harvestable find, then plateau.

## Validated structure (keep on next attempt)
- Section 4 (frog y-nudge) nests INSIDE `if (GetFlag(...)) { if ((_field_10 & 2)
  && i == colliderTag) { switch...; <section4> } }` — NOT at loop level
  (baserom `b.n 0x23F2E` at 0x23D70/82/8A proves it).
- Allocation flip: route the GetFlag result through `f` + `(s8)f` test — one
  flow-time insn in the idx-only window flips idx→r7/st→r6, zero bytes.
- `(status|2) & 0x7FFF` narrows to an HImode pool const only while the 0x8000
  test stays `hi & status` (reg-based); `status & hi` lets cse fold it.
- case-2 `subs` form needs the multi-set temp `m = hi; ... & (m -= 1)` inline
  (unmovable); plain `hi - 1` gets hoisted and rematerialized.
- 2-constant partition ({case3-state, both |2} → sl) emerges from plain
  literals; ANY 2-valued variable inverts it.

## Best-effort C
`.agent-session-save-20260609/sub_08023BC0_best_effort_103.c` (byte_diff 103)
and `_286.c` (exact case-2 test shape, +cascade), `sub_08023BC0_analysis.md`
(full session analysis) — repo root, untracked.

## Next lever
A `register` pin on the const pseudo (historical precedent: sub_08021510) or
codex-tier escalation. The two unconstructible clusters are reload-level insn
shapes, not allocation-order — the cast/perturbation levers cannot reach them.
