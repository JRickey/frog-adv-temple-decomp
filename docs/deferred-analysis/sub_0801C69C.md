# Deferred analysis: sub_0801C69C

Range: [0x0801c69c, 0x0801c6e4) — 72 bytes, Thumb. Dest: src/engine/sub_0801c464.c.

## Function semantics

Clears DISPCNT.WIN0_ON (bit 13), zeros WIN0H (0x04000040) + WIN0V (0x04000044),
zeros palette[0] (0x05000000), then DMA3-fills BG screenblock 31 (0x0600f800,
0x400 halfwords = 2048 bytes) with 0 from a stack local (DMA_SRC_FIXED), reading
DMA3.cnt back once, and finally clears DISPCNT.BG3_ON (bit 11).

Takes a `u8 a` parameter (callers at ROM 0x1a416/0x1a488/0x1a4aa/0x1a5ba pass
`adds r0, r5, #0`, r5 = a state value, can be non-zero e.g. 14). The sibling
`sub_0801C6FC` at 0x1c6fc has the SAME prologue but narrows `a` into r3 and
`switch`es on it (`cmp r3,#15; ... mov pc,r0`). In 0801C69C, `a` is narrowed
into r0 and then **never read** — the body fills with 0, not `a`.

## ROOT CAUSE — why this is NOT matchable in clean pure C (NEW, agbcc-source proof)

This is the same byte_diff-34 plateau the first attempt hit, but here is the
mechanism, derived from the agbcc 2.x source (tools/agbcc-src), not guessed:

The target's prologue is `sub sp,#4; lsls r0,#24; lsrs r0,#24` — the standard
`u8`-param narrow into r0. Empirically (probed both old_agbcc AND agbcc):

  **agbcc NEVER emits this narrow for a fully-dead `u8` param.** A dead param's
  use is eliminated and the narrow with it. So `a` MUST be referenced.

The ONLY reference that produces exactly `lsls#24;lsrs#24` + the observed shape
is `local = a` (store the narrowed param to the u16 stack slot). But the baserom
stores **0** to local (`movs r0,#0; strh r0,[sp]`), not `a`. So the source must
be `local = a; local = 0;` — and the first (dead) store must vanish, leaving the
narrow.

  **agbcc structurally REFUSES to dead-store-eliminate a STACK-SLOT store.**
  flow.c `mark_set_1` (~line 2393):

      if (GET_CODE (reg) == MEM && ! side_effects_p (reg)
          && ! reg_mentioned_p (stack_pointer_rtx, reg))   /* <-- */
        mem_set_list = gen_rtx_EXPR_LIST (VOIDmode, reg, mem_set_list);

  A MEM store is added to the dead-store tracking list (`mem_set_list`) ONLY if
  its address does NOT mention the stack pointer. `local` is `[sp]`, so `local=0`
  is never tracked, so the earlier `local=a` can never be marked dead
  (insn_dead_p, ~line 2057, only fires on an identical *tracked* later store).
  The flow.c comment is explicit: "don't eliminate any stores through SP."

Consequence: `local=a; local=0` ALWAYS emits BOTH `strh`s (byte_diff 34 floor),
while the target has ONE store of 0 + a DISPCNT-base reload. No clean C, register
pin, per-TU flag, or compiler swap bridges this — confirmed exhaustively below.

## Drift: byte_diff 34, diff_count 11 (floor; same as prior attempt)

The two residual differences vs target, after `local=a;local=0`:
1. EXTRA `strh r0,[r1]` (the dead `local=a` store agbcc can't DSE — see root cause).
2. The 2nd DISPCNT base CSEs (`ldrh r1,[r3]`) where target RELOADS
   (`movs r2,#128; lsls r2,#19; ldrh r1,[r2]`). The reload only appears when `a`
   genuinely occupies r0 DEAD across the body (never read); when `a` is read by a
   store, agbcc keeps r3 live and CSEs. `0x04000000` is a `thumb_shiftable_const`
   (config/arm/thumb.c) so agbcc rematerializes it cheaply — but only under the
   exact live-range profile of a dead-in-r0 param, which can't be produced cleanly.

`local = a` alone (fill with `a`) is byte_diff 48 — closer on instruction count
(only 3 extra), but it's the WRONG semantic (fills with `a`, not 0) and stores
`a` where target stores 0, so it is not a real base either.

## Levers tried this pass (all fail — do NOT re-try these)

- `local = a; local = 0;` (both orders): byte_diff 34. Two stores (root cause).
- `local = a` (single, fill=a): byte_diff 48. Wrong value + CSE, no reload.
- `local = 0` (a unused): byte_diff 79. Drops narrow, whole alloc shifts (r2first).
- palette = a / WIN = a / DISPCNT-mask |= a : narrow appears but stores `a` to the
  wrong MMIO reg (wrong bytes); none give the reload.
- `a << 8 << 8` in the FINAL DISPCNT mask: DOES reproduce narrow + reload(!), but
  agbcc emits the runtime `lsr r0,#8; orr; bic` shift code (a<<16) instead of the
  plain `ands 0xf7ff` — byte_diff 59. This is the only construct that triggers the
  reload, and it always emits extra `a` instructions the target lacks.
- `(u16)(a<<8<<8)` / `a&0x10000` / `a<<8<<8<<8` : all const-fold to 0 → no narrow.
- `a ? 0 : 0`, `(a,0)`, `a==a?0:0`, `if(a){}`, `(void)a`, `a=a`, dead `fill=a`:
  all eliminated → no narrow.
- `{volatile u8 z=a;}` : narrow survives but adds a 2nd stack slot + strb/ldrb.
- `asm("" :: "r"(a))` barrier (various placements): keeps narrow but swaps the
  local-store register order, emits `.code 16` debt, and never produces the reload.
- per-TU flags swept on the `local=a` base for the reload: -fno-gcse, -fforce-addr,
  -fno-expensive-optimizations, -fno-cse-follow-jumps, -fno-cse-skip-blocks,
  -fno-function-cse, -fno-strength-reduce, -O1, -fno-schedule-insns,
  -fno-rerun-cse-after-loop, -fno-caller-saves — NONE produce base-synth=2 (reload).
- Newer `agbcc` (vs default old_agbcc): identical (no dead-param narrow, CSEs).

## Conclusion / next attempt

This is a genuine clean-C impossibility under agbcc (dead-narrowed stack param +
SP-store DSE refusal). It is NOT STRONG_UNMATCHABLE (no libgcc class), so it stays
an honest asm slice rather than a fake NAKED ship.

Only remaining theoretical paths for a future attempt (all unlikely / not clean):
- A NAKED + NON_MATCHING ship (hand-asm) IF policy ever allows it for this class.
- A future agbcc-flag discovery that narrows a dead `u8` param (none found in the
  whole `-fXXX` surface) — would let `local = 0; (void)a;` keep the narrow.
- A source form that puts `a` dead-in-r0 with zero emitted code AND triggers the
  shiftable-const reload — none exists in the agbcc cost model as analyzed.

## Best-effort C (byte_diff 34 floor — readable, two-store)

```c
void sub_0801C69C(u8 a)
{
    u16 local;

    REG_DISPCNT &= ~DISPCNT_WIN0_ON;
    *(vu16 *)0x04000040 = 0;
    *(vu16 *)0x04000044 = 0;
    *(vu16 *)0x05000000 = 0;
    local = a; /* dead store agbcc cannot DSE (SP-slot); see root cause */
    local = 0;
    REG_DMA3.src = &local;
    REG_DMA3.dst = (void *)0x0600f800;
    REG_DMA3.cnt = DMA_ENABLE | DMA_SRC_FIXED | 0x400;
    (void)REG_DMA3.cnt;
    REG_DISPCNT &= ~DISPCNT_BG3_ON;
}
```


---

## Corpus sweep (2026-06-03)

### The exact asm idiom that diverges
Baserom `sub_0801C69C` [0x1c69c,0x1c6e4) is a 72-byte leaf that clears DISPCNT WIN0 bit, zeros WIN0H/WIN0V/palette[0], DMA-fills screenblock 31 (0x0600F800, 2048B) with 0 from a stack local, then clears DISPCNT BG3 bit. It takes a `u8 a` param.

Two coupled divergences, both rooted in ONE agbcc behavior:

1. **Dead-but-extended param.** Baserom emits `lsls r0,#24; lsrs r0,#24` (u8 zero-extension of `a`) at the very top, keeps `a` in r0 across the ENTIRE window/palette section, then at the local store does `movs r0,#0` — materializing a FRESH 0 in r0 (reusing a's just-freed register) rather than reusing the existing r2=0 from the palette write. So `a`'s value is NEVER read; only its liveness pins the register allocation.

2. **Second DISPCNT base recompute (NOT CSE).** Baserom recomputes the 0x04000000 base for the second `&=` (`movs r2,#128; lsls r2,#19; ldrh r1,[r2]`), instead of CSE-ing the r3 it loaded for the first access. This recompute is a downstream consequence of the register pressure from `a` being live across the body.

### Every regex / grep searched and hit counts
- `corpus_asm_search.py --asm 'mov\s+r[0-7],\s*sp\n\s*mov[s]?\s+r[0-7],\s*#0\n\s*strh\s+r[0-7]'` --require-c → **0 hits** (the tool matches per-LINE, so `\n` can never match; multiline idioms are not searchable this way — noted for next agent).
- C-tree grep `DCNT_WIN0|DISPCNT.*WIN0|&= ~.*WIN0` over ~/.cache/decomp-corpus → ~20 files (mf/ending, mksc/title, many pokefirered) — all window-clears but every one with a USED param.
- C-tree grep `DmaFill|DMA_FILL|FillVram` → pokeemerald macro.h + cvaos (10+ files). 
- C-tree grep `0x0600F800|0600E000|SCREEN_BASE_BLOCK(31)` → cvaos code_080109F4.c, code_08005894.c, pokepinballrs/intro.c, katam.

### Top corpus hits examined (repo@sha — one line each)
- **testyourmine/cvaos@29f76ec118 include/gba/dma.h** — the `DMA_FILL(channel,val,dst,size,bit)` macro: `vu##bit dma_tmp_=(vu##bit)val; DMA_SET(channel,&dma_tmp_,dst,(DMA_ENABLE|DMA_START_NOW|DMA_16BIT|DMA_SRC_FIXED|DMA_DEST_INC)<<16 | size/(bit/8));` where DMA_SET does `dma_[0]=src; dma_[1]=dst; dma_[2]=cnt; dma_[2];`. This is the EXACT shape of the baserom bottom half (incl. the `(void)cnt` readback `ldr r0,[r1,#8]`). With val=0 the cnt literal computes to `0x81000400` — byte-identical to the baserom pool. **This trick transferred for the bottom half / DMA section.**
- **cvaos sub_08011AD0 / sub_08011B2C (code_080109F4.c)** — `gUnk_03002CB0.dispCnt &= ~DCNT_WIN0;` then `DMA_FILL_32(3,0,0x0600F800,0x800)`. Structural twin of our function, but param is a USED `struct EwramData_unk60*` — no dead-param artifact.
- **cvaos sub_08010144 (code_0800F1FC.c)** — same dispCnt-clear + DMA_FILL + window-register zeroing; param used.

### Whether/why the trick transferred
PARTIAL. The cvaos DMA_FILL idiom is the correct C for the DMA section and (with value 0) for the `tmp=0` store — and indeed my near-match's TOP HALF (WIN0 clear, WIN0H/WIN0V=0, palette=0) is already byte-perfect, and the DMA section matches. What the corpus does NOT contain is prior art for a **dead-but-extended `u8` param** — because decomp authors never write a param that's unused, that exact agbcc artifact simply doesn't appear in any matched corpus C.

I proved the coupling empirically (15+ variants via a fast preproc|cpp-15|old_agbcc|as harness):
- No use of `a` → extension DCE'd entirely AND the whole body reorders (byte_diff large).
- `(void)a;`, `a;`, `if(a){}`, `switch(a)`, dead scalar `t=a` → all DCE'd, no extension.
- `a&0`, `a-a`, `a>>8` → folded to 0, `a` dead, no extension.
- `a=0;` (reassign) → `a` dead, no extension.
- The ONLY thing that preserves the extension is storing `a` to an **address-taken local** (`local=a`), and agbcc **never DCEs a store to an address-taken local** (confirmed across `-fno-gcse -fforce-addr -fno-cse-follow-jumps -O1 -fno-expensive-optimizations -fno-defer-pop` — the dead `local=a` store survives every flag). So keeping `a` live ALWAYS costs the extra `strh r0,[sp]`, and the baserom has NO such store (it stores only 0). These two requirements are mutually exclusive in agbcc-from-C.
- `&a` of a u8 param → `strb` (byte) not `strh`, and no extension — wrong (matches doc's prior note).

### agbcc pass implicated
Register-coloring/liveness root in `tools/agbcc-src/gcc_arm/`: the param promotion/extension survival is governed by liveness in **flow.c** (dead-store elimination refuses to remove stores to address-taken `MEM`s) and **local-alloc.c / reload.c** (the fresh `movs r0,#0` into a's freed register, and the choice to recompute the DISPCNT base into r2 rather than keep r3 live across the DMA). The second-DISPCNT recompute is **cse.c / gcse.c** declining to extend the base-constant's live range across the DMA block — but it is a *consequence* of the liveness pattern, not independently flag-controllable (no `-fno-gcse`/`-fno-cse*` variant broke it).

### byte_diff progression
asm-only → 36 (baseline `local=0; local=a`) → **34** (variant C `local=a; local=0`, the near-match base; perfect top half + correct DMA section, residual = the dead `local=a` store at +0x24/+0x28 and the r3-CSE at +0x38). Variant F (`local=a` only, fill with a) = 48 (worse — missing `movs r0,#0` shifts the whole pool). Doc's prior permuter run: 2000+ iters, best score 460, never 0.

### RECOMMENDED NEXT ANGLE
The clean-C route is essentially exhausted; the blocker is a structural agbcc coupling with no C trigger and no corpus precedent. Three concrete avenues, in priority order:
1. **Re-examine the call ABI assumption.** Every call site is `adds r0,r5,#0; bl 0x1c69c` and the caller's r5 is a live value (`subs r0,r5,#1` right after). BUT verify whether the function is reached through a **typed function-pointer table** whose signature is `void(*)(u8)` while THIS body genuinely ignores the arg — if so, the matching source may legitimately be `void sub_0801C69C(u8 a)` with `a` referenced via a construct that the project's OTHER TUs use to keep a forced-but-dead param (search the project's already-matched functions for any that take a param, ignore it, yet emit `lsl#24;lsr#24` — if even one exists in src/, copy its exact C shape).
2. **Try the param as the DMA fill value with a forced post-zero that agbcc folds into the SAME store.** Specifically attempt a `vu16 tmp; tmp = a; ... ` where the DMA tmp and the `a`-sink are unified and the final value is 0 via a volatile re-read — i.e. expand the cvaos DMA_FILL macro by hand with `val` being an expression that uses `a` but whose volatile-qualified result is 0, so agbcc must compute (extend) `a` yet store 0. (My fold tests used non-volatile; a `vu16` intermediate may behave differently.)
3. **Last resort: permuter from the variant-C seed (byte_diff 34) preserved in nearMatchBase**, but bound it — the prior 2000-iter run plateaued at 460, so cap at ~15 min and only as confirmation, not expectation. If still 0-less, this is a legitimate NAKED+NON_MATCHING candidate (it is the rare narrow case: a register pinned by a *dead* param across a DMA block — adjacent to the documented "high reg holding loop state across BL" unmatchable class).

### Near-match C base (permuter seed) — byte_diff 36->34, CFLAGS: (default -O2)

```c
#include "gba/dma.h"
#include "gba/io.h"
#include "types.h"
void sub_0801C69C(u8 a)
{
    u16 local;
    REG_DISPCNT &= ~DISPCNT_WIN0_ON;
    *(vu16 *)0x04000040 = 0;
    *(vu16 *)0x04000044 = 0;
    *(vu16 *)0x05000000 = 0;
    local = a;
    local = 0;
    REG_DMA3.src = &local;
    REG_DMA3.dst = (void *)0x0600F800;
    REG_DMA3.cnt = DMA_ENABLE | DMA_SRC_FIXED | 0x400;
    (void)REG_DMA3.cnt;
    REG_DISPCNT &= ~DISPCNT_BG3_ON;
}
```
