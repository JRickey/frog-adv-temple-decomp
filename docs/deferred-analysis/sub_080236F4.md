# sub_080236F4 (0x080236F4, 960 bytes) — DEFERRED (pure-C constraint run, 2026-06-09)

Attempted under a no-pin/no-asm/no-NAKED constraint; agent reverted cleanly.

## Drift
- Best: byte_diff 785 raw, but normalized instruction diff **85/828 (90% matching)**;
  size 940 vs 960. Control flow, case-body physical order, pool constants, call
  marshalling, hoisted invariants (maskHi→sl, mask8→sl, maskHi2→r8, slot→r8/r9)
  all byte-exact.
- Diverges on (all register-orientation, zero semantic):
  1. loop1 case0/case1 duplicate 17-insn bodies: baserom keeps BOTH copies only
     because reload's round-robin scratch rotation (reload1.c `last_spill_reg`)
     gave their 0x7FFF pool loads different scratch regs (r3 vs r2); our builds
     rotate ≡0 there, so post-reload cross-jump merges them (−20 bytes).
     Source-level goto-sharing reproduces the internals but merges; unshared
     forms survive but tie the orr in-place.
  2. or-tie orientation at ~4 stores: agbcc coalesces or-result into the dying
     status pseudo (`orrs r1,r2` in-place); baserom copies const side first
     (`adds r0,r2,#0; orrs r0,r1`).
  3. loop1 case3 AND-tie: copy-from-status vs copy-from-two.
  4. loop2 e2/two/base callee-saved rotation: built {two=r4, base=r5, e2=r6}
     vs target {e2=r4, two=r5, base=r6}.
- Levers tried: two-variable slot split (loop-top byte-exact — KEEP); per-case
  status temps (state→r2 dispatch match — KEEP); per-loop e/state splits;
  goto-shared tails; type flips; linker-alias decoupling; per-TU CFLAGS;
  AGBCC_BIN swap (worse: 149 vs 85); permuter 3 restarts ~20k iters (plateau
  ~3%; mirrored target can't represent a match while sizes differ);
  combine-foldable-cast lever (greg stats IDENTICAL — these ties are local-alloc
  qty-coalescing + reload rotation, NOT global-alloc priority, so that lever
  cannot reach them).
- Family note: siblings sub_08022434 / sub_08022fec are NAKED; sub_08021510
  matched only via a register pin + inline asm — same orientation class.

## Best-effort C
The 90%-matching source is preserved verbatim at
`.agent-session-save-20260609/v199_sub_080236F4_best_effort.c` (repo root,
untracked) and in the agent transcript. Key shapes to keep on the next
attempt: the `slotIdx`/`slot` assign-then-save split (produces the baserom's
`adds rX,r7,#0; adds rX,#imm … mov r8/r9,rX` loop-top), per-case status temps,
per-loop e/state splits, `(((i % 5u) << 24) >> 4) + 0x02910000) >> 16` anim-id
form, and the two Entity_AdvanceAnimFrames + Entity_InitFromLayout x3 tail.

## Next lever to try
Reproduce reload's scratch rotation state: the rotation counter advances with
each preceding spill-reload in the function, so inserting/removing an earlier
spill (e.g. via a dead combine-foldable cast that forces one extra reload
before case 0) may rotate the 0x7FFF scratch from r2 to r3 and let both
duplicate bodies survive cross-jump. Alternatively escalate to a pin-allowed
tier (the goal that produced this run forbade pins).
