# sub_08012A6C - deferred (reload/local-alloc plateau)

204 bytes @ 0x08012a6c, asm slice `asm/disasm_0x08012a6c.s`, intended dest
`src/engine/sub_08012a6c.c`. `classify_unmatchable.py` verdict after refreshing
the address snapshot: **ATTEMPT_MATCH**. Advisory only: one high register across
the blit call; not a NAKED class.

## Drift

Best known result remains **byte_diff 101, diff_count 37** with OLD_AGBCC, no
inline asm, and two narrow register locals:

- `flag` pinned to `r9`, matching the baserom sentinel pointer.
- `one` pinned to `r1` in a small block to force a fresh `mov r1, #1` mask for
  `bank & 1`.

This session re-derived from a clean readable C body and did not beat that
plateau. New paths ruled out:

- Clean readable C with shared tail: **174/75**. It fixes the early literal pool,
  but allocator still chooses `state=r7`, `desc=r6`, `dst=r8`, `frames=ip`.
- Loading the descriptor after `tick - state->lastTick` matches the target load
  point, but remains **176/75** with the same state/descriptor swap.
- Pinning low registers for `state r6` / `desc r7` is counterproductive: explicit
  local register variables are excluded from normal saved-register liveness, so
  the prologue loses `r7` and worsens to **133/73** despite some local fixes.
- Pinning only high/caller-scratch values (`flag r9`, `dst ip`, `frames r8`)
  gives the desired `dst`/`frames` roles but still lacks the saved `sl` slot and
  remains **175/75**.
- `volatile u32 bank` creates the desired 16-byte frame and `[sp,#12]` stack
  home, but damages register coloring (`state` moves through `r8`) and regresses
  to **151/89**.
- `one asm("r1")` reproduces the bank-mask copy but forces the initial sentinel
  read into `r0`; with the high/caller-scratch pins it lands at **176/72**.
- New agbcc for this TU emits the same shape as OLD_AGBCC on the tested variant.
- Per-command flag sweep on the current best-effort shape did not help:
  `-ffixed-r4/r5/r6/r7` regressed to **180/101**; `-ffixed-r8/r9/r10` gave
  **174/72**; `-fno-gcse` and `-fforce-addr` stayed **176/72**;
  `-fno-expensive-optimizations` gave **174/74**.
- Required corpus checks found no C replacement for the relevant history idioms:
  high-register move search completed with total hits 0; stack-home
  `str rN, [sp,#12]` history search completed with total hits 0.

Passes implicated: `regclass.c` sets pseudo preferences, `local-alloc.c` records
the memory/constant equivalences and local register priorities, and
`reload1.c:allocate_reload_reg` chooses spill/reload registers round-robin. The
remaining mismatch is allocator/reload shape: stack-homing `bank`, saving unused
`sl`, and the `state`/`desc` plus `dst`/`frames` rotations.

Next likely useful step: instrument a private `old_agbcc` in `reload1.c` around
reload-register allocation for the `bank` pseudo and the frame-table pseudo. The
no-rebuild `.greg` dump shows useful pseudos but not why reload refuses the
target stack-home/`r8` combination.

## Best-effort C

```c
#include "game.h"
#include "types.h"

struct ScaleAnimDescriptor {
    u16 field_00;
    u16 field_02;
    u8 field_04;
    u8 pad_05;
    u8 field_06;
    u8 pad_07;
    u8 field_08;
    u8 pad_09;
    u16 frameCount;
    u8 timeStep;
    u8 pad_0D[3];
    const u32 *frames;
    u32 pad_14;
};

struct ScaleAnimState {
    u8 pad_00[4];
    u32 lastTick;
    u8 pad_08[2];
    u8 frameIndex;
};

extern u8 gIwram_3610;
extern const struct ScaleAnimDescriptor sScaleAnimDesc_6e64;

extern void sub_0801025C(u8 rows, u8 cols, u16 x, u16 y, u32 bank, u32 src, u32 dst);
extern void sub_0800E7D4(void);

void sub_08012A6C(void)
{
    struct ScaleAnimState *state;
    const struct ScaleAnimDescriptor *desc;
    GameStuff *gs;
    u32 tick;
    u32 bank;
    u32 dst;
    const u32 *frames;
    u8 rows;
    u8 cols;
    u8 *coords;
    u16 x;
    u16 y;
    u32 frameIndex;
    register u8 *flag asm("r9") = &gIwram_3610;

    if (*flag != 1)
        goto done;

    gs = &gGameStuff;
    state = (struct ScaleAnimState *)0x030064C0;
    tick = gs->_unk00;
    desc = &sScaleAnimDesc_6e64;
    if (tick - state->lastTick < desc->timeStep)
        return;

    state->lastTick = tick;

    bank = desc->field_08 >> 4;
    {
        register u32 one asm("r1");

        one = 1;
        dst = 0x0600E000;
        if (bank & one)
            dst = 0x0600E800;
    }

    frames = desc->frames;
    rows = desc->field_06;
    cols = desc->field_04;
    coords = (u8 *)0x03003720;
    x = *(s16 *)(coords + 0x692) * 3;
    y = *(s16 *)(coords + 0x694) * 3;

    frameIndex = state->frameIndex;
    state->frameIndex = frameIndex + 1;
    sub_0801025C(rows, cols, x, y, bank, frames[frameIndex], dst);

    if (state->frameIndex >= desc->frameCount)
        *flag = 0;

done:
    sub_0800E7D4();
}
```
